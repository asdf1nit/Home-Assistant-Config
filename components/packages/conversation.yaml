---
# The conversation component allows you to converse with Home Assistant. 
# You can either converse by pressing the microphone in the frontend (supported browsers only (no iOS)) or by calling the conversation/process service with the transcribed text.
#
# Allows you to issue voice commands from the frontend in enabled browsers
conversation: